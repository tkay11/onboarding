.panel
  .user-card
    .user-card__body
      %p.user-card__title
        = @poll.title
      %p.user-card__position
        = @poll.description

%p
  %b Questions:
  = @poll_questions.count

= render "list"

= link_to 'New question', new_poll_question_path(@poll)
